1e. when the vector capacity is reached, the old container is deleted and a new one is allocated, so the old iterators p and v.end() are invalidated and cannot be called.
3. There is a build error because when a Map<Coord, int> calls insert, it calls findFirstAtLeast which compares key values with an operator. The coord class has not defined a < operator so its unable compare two KeyType Coord Objects, hence it does not compile.
4b. Because the recursive function must accumulate the domain name string as it traverses the tree, sso without a second parameter to hold the path (without using global vars), there is no way to store that info. THis is why recrusion needs 2 parameters.
5a. O(N^3)
5b. Its still O(N^3) since in the middle loop, the max value J can be is N. Since we only care about the case where J is the max for calcualting big-O, O(N*N*N) = O(N^2).
6a. its O(N^2) since insert() is a O(N) operation (it uses a linear search through the list to find the correct spot while insertion is constant), and it has a oter for loop so its n^2.
6b. Its o(N) because it goes through the entire linked list just once. Its better than part A.

